<%= form_for @link, url: link_path(@link) do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.label :url %>
  <%= f.text_field :url %>
  <%= f.submit 'Update', class: 'btn btn-success' %>
<% end %>